async function giveMoneyLimitation(msg, bot, collection){

}

async function promoMoneyLimitaion(msg, bot, collection, collectionPromo) {
    const chatId = msg.chat.id
    const userId1 = msg.from.id
    const text = msg.text
}